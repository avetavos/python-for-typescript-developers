---
const { title = "No TypeScript equivalent" } = Astro.props;
---
<aside class="go-only">
  <p class="go-only__label">🟦 {title}</p>
  <slot />
</aside>
<style>
  .go-only { border-left: 4px solid #00ADD8; background: var(--sl-color-accent-low); padding: 0.75rem 1rem; border-radius: 0 6px 6px 0; margin: 1rem 0; }
  .go-only__label { font-weight: 700; margin: 0 0 .25rem; color: var(--sl-color-accent-high); }
</style>
