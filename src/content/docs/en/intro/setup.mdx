---
title: Setting Up Go
description: Install Go and learn the essential CLI commands — go run, go build, go mod init, go install — compared to node, tsc, and npm.
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## Setting up your Go environment

Setting up TypeScript requires Node.js, npm (or another package manager), and `tsc`. Go has a single, unified toolchain — you install Go once and get everything: the compiler, formatter, test runner, and module manager.

## Installing Go

Download the official installer from [go.dev/dl](https://go.dev/dl/). After installation, verify with:

```bash
go version
# go version go1.22.0 darwin/arm64
```

Go installs to `/usr/local/go` by default. The `GOPATH` (workspace directory, defaults to `~/go`) is where downloaded modules and installed binaries live. You rarely need to think about it — `go mod` handles everything.

## The Go CLI vs the Node/npm CLI

Every Node.js developer knows `node`, `npm`, and `tsc`. Go consolidates all of these into the single `go` command.

export const tsCliCode = `# TypeScript / Node.js toolchain
node index.js            # run a file
npx ts-node src/index.ts # run TypeScript directly
tsc                      # compile TypeScript
npm install              # install dependencies
npm run build            # run a build script
npx <tool>              # run a package binary`;

export const goCliCode = `# Go toolchain (all one command)
go run main.go           # compile + run in one step
go run .                 # run the current package
go build -o myapp .      # compile to a binary
go mod init github.com/you/myapp  # init a new module
go get github.com/gin-gonic/gin   # add a dependency
go install tool@latest   # install a CLI tool globally`;

<TsGo client:visible ts={tsCliCode} go={goCliCode} />

## go run vs go build

`go run` is your development loop — it compiles and executes in one step, like `ts-node` or `npx tsx`. Use it while developing. `go build` produces a standalone binary you can deploy. There is no separate "watch mode" built in, but tools like [air](https://github.com/air-verse/air) provide live reload.

export const tsRunCode = `# TypeScript development loop
npx ts-node src/index.ts    # compile + run (dev)
tsc && node dist/index.js   # compile then run (CI/prod)
npx tsx watch src/index.ts  # watch mode`;

export const goRunCode = `# Go development loop
go run .                    # compile + run (dev)
go build -o ./bin/app . && ./bin/app  # compile then run
# air                       # watch mode (third-party)`;

<TsGo client:visible ts={tsRunCode} go={goRunCode} />

## go mod init — the equivalent of npm init

When you start a new Go project, you initialize a module with `go mod init`. This creates a `go.mod` file — the Go equivalent of `package.json`.

export const tsNpmInitCode = `# TypeScript
npm init -y
# creates package.json with name, version, deps

# then add a dependency:
npm install express
# updates package.json + creates package-lock.json`;

export const goModInitCode = `# Go
go mod init github.com/yourname/myapp
# creates go.mod

# then add a dependency:
go get github.com/gin-gonic/gin
# updates go.mod + creates go.sum (like package-lock.json)`;

<TsGo client:visible ts={tsNpmInitCode} go={goModInitCode} />

## go install — global CLI tools

`npm install -g` installs a CLI tool globally. `go install` does the same — it compiles the module and places the binary in `$GOPATH/bin` (add this to your `PATH`).

export const tsInstallCode = `# TypeScript: global CLI tools
npm install -g typescript
npm install -g prettier
npx create-react-app myapp`;

export const goInstallCode = `# Go: global CLI tools
go install golang.org/x/tools/gopls@latest      # LSP server
go install github.com/air-verse/air@latest       # live reload
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest`;

<TsGo client:visible ts={tsInstallCode} go={goInstallCode} />

<GoOnly title="GOPATH, GOROOT, and module cache">

You will occasionally see these environment variables referenced:

| Variable | Default | What it is |
|----------|---------|------------|
| `GOROOT` | `/usr/local/go` | Where the Go toolchain is installed |
| `GOPATH` | `~/go` | Your workspace — module cache lives in `~/go/pkg/mod/` |
| `GOBIN` | `$GOPATH/bin` | Where `go install` puts binaries |

Run `go env` to see all Go environment variables. Run `go env GOPATH` for a single value. You rarely need to change these — `go mod` makes the module cache transparent.

```bash
# useful diagnostics
go env          # print all Go environment variables
go env GOPATH   # print just GOPATH
go version      # print installed Go version
go doc fmt      # read the fmt package docs in the terminal
```

</GoOnly>

## Playground

This is a setup lesson — the Playground is less meaningful here (there is nothing to install in the browser), but you can use the playground on the next lesson (Hello World) to run your first complete Go program.

export const quizQuestionsSetup = [
  { q: 'What is the Go equivalent of running `ts-node src/index.ts`?', options: ['go install .', 'go build .', 'go run .', 'go test .'], answer: 2 },
  { q: 'What file does `go mod init` create?', options: ['go.json', 'package.go', 'go.mod', 'go.sum'], answer: 2 },
  { q: 'Where does `go install` place compiled binaries?', options: ['./bin/', '/usr/local/bin/', '$GOPATH/bin', './node_modules/.bin/'], answer: 2 },
  { q: 'What is go.sum equivalent to in the Node.js ecosystem?', options: ['package.json', '.npmrc', 'package-lock.json', '.yarnrc'], answer: 2 },
];

<Quiz client:visible id="intro/setup" questions={quizQuestionsSetup} />

<ProgressTracker client:visible id="intro/setup" />
