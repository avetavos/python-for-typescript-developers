---
title: Pydantic Models
description: การกำหนด request และ response schema ด้วย Pydantic BaseModel เปรียบเทียบกับ Zod schemas และ TypeScript interfaces
---
import TsGo from '../../../../components/TsGo.astro';
import Playground from '../../../../components/Playground.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import PyOnly from '../../../../components/PyOnly.astro';

## Schemas: Zod / interfaces → Pydantic BaseModel

ใน TypeScript คุณอธิบายรูปร่าง request body ด้วย interface หรือ Zod schema ใน FastAPI คุณอธิบายด้วย subclass ของ **Pydantic `BaseModel`** ส่ง class นั้นเป็น type hint บน argument ของ route function และ FastAPI จะ parse และ validate request body อัตโนมัติ

export const schemaTs = `// TypeScript — Zod schema + inferred type
import { z } from 'zod';

const CreateItemSchema = z.object({
  name: z.string().min(1),
  price: z.number().positive(),
  inStock: z.boolean().default(true),
  tags: z.array(z.string()).default([]),
});

type CreateItem = z.infer<typeof CreateItemSchema>;

// NestJS DTO with class-validator
export class CreateItemDto {
  @IsString() @MinLength(1) name: string;
  @IsNumber() @IsPositive() price: number;
  @IsBoolean() @IsOptional() inStock?: boolean = true;
}`;

export const schemaPy = `# FastAPI — Pydantic BaseModel
from pydantic import BaseModel

class CreateItem(BaseModel):
    name: str
    price: float
    in_stock: bool = True
    tags: list[str] = []

# ใช้ใน route — FastAPI parse + validate อัตโนมัติ
from fastapi import FastAPI
app = FastAPI()

@app.post("/items", status_code=201)
async def create_item(item: CreateItem):
    return item          # Pydantic serialize กลับเป็น JSON`;

<TsGo ts={schemaTs} go={schemaPy} />

## Nested models

Pydantic models รวมกันได้อย่างเป็นธรรมชาติ ฝัง model หนึ่งไว้ใน model อื่นเหมือน nested Zod objects

export const nestedTs = `// Zod nested object
const AddressSchema = z.object({
  street: z.string(),
  city: z.string(),
  zip: z.string().length(5),
});

const UserSchema = z.object({
  name: z.string(),
  address: AddressSchema,
});`;

export const nestedPy = `# Pydantic nested models
from pydantic import BaseModel

class Address(BaseModel):
    street: str
    city: str
    zip_code: str

class User(BaseModel):
    name: str
    address: Address

# FastAPI จะ parse nested JSON นี้อัตโนมัติ:
# { "name": "Alice", "address": { "street": "1 Main St", ... } }`;

<TsGo ts={nestedTs} go={nestedPy} />

## Response models — แยก input จาก output

รูปแบบที่พบบ่อยคือมี model แยกกันสำหรับ input (สิ่งที่ client ส่งมา) และ output (สิ่งที่คุณส่งกลับ) Pydantic ทำให้ compose ง่ายมาก

export const responseModelTs = `// TypeScript — แยก input / output types
interface CreateItemDto { name: string; price: number; }
interface ItemResponse { id: number; name: string; price: number; createdAt: string; }`;

export const responseModelPy = `# Pydantic — แยก input / output models
from pydantic import BaseModel
from datetime import datetime, UTC

class CreateItem(BaseModel):       # Request body
    name: str
    price: float

class ItemResponse(BaseModel):     # Response shape
    id: int
    name: str
    price: float
    created_at: datetime

# Route ใช้ response_model= เพื่อ filter + serialize
from fastapi import FastAPI
app = FastAPI()

@app.post("/items", response_model=ItemResponse, status_code=201)
async def create_item(item: CreateItem):
    # ปกติจะบันทึกลง DB แล้ว return row
    return ItemResponse(id=1, name=item.name, price=item.price,
                        created_at=datetime.now(UTC))`;

<TsGo ts={responseModelTs} go={responseModelPy} />

<PyOnly title="model_dump() และ model_validate() — Pydantic v2 methods">

Pydantic v2 (เวอร์ชันปัจจุบัน) เปลี่ยนชื่อ `.dict()` → `.model_dump()` และ `.parse_obj()` → `.model_validate()` คุณอาจเห็น code เก่าใช้ `.dict()` ยังใช้ได้แต่ deprecated แล้ว

```python
item = CreateItem(name="Widget", price=9.99)

# Serialize เป็น dict
data = item.model_dump()
# {'name': 'Widget', 'price': 9.99, 'in_stock': True, 'tags': []}

# Deserialize จาก dict
item2 = CreateItem.model_validate({"name": "Gadget", "price": 4.99})
```

</PyOnly>

## Forward reference: ไม่ต้องใส่ quote อีกแล้ว

ใน TypeScript type หนึ่งอ้างถึง type ที่ประกาศทีหลังในไฟล์เดียวกันได้ เพราะ compiler resolve type หลัง parse เสร็จ ส่วน Python เดิมประเมิน annotation ทันที model ที่อ้างถึงตัวเองหรืออ้างถึงชื่อที่ยังไม่ถูกประกาศจึงต้องเขียนเป็น string แล้ว Pydantic ต้องเรียก `model_rebuild()` ตามทีหลัง

Python 3.14 ประเมิน annotation แบบ lazy (PEP 649/749) การอ้างถึงชื่อที่นิยามทีหลังใน module เดียวกันจึงใช้ได้เลย และไม่ต้องพึ่ง `from __future__ import annotations` เพื่อเรื่องนี้อีก

export const forwardRefPy = `# Python 3.14 — ไม่ต้อง quote ไม่ต้อง __future__ import
from pydantic import BaseModel

class Comment(BaseModel):
    text: str
    replies: list[Comment] = []      # อ้างถึงตัวเอง: ใช้ได้บน 3.14

class Order(BaseModel):
    customer: Customer               # นิยามอยู่ข้างล่าง: ใช้ได้เช่นกัน

class Customer(BaseModel):
    name: str

# ก่อน 3.14 ต้องเขียน annotation เป็น string:
#   replies: list["Comment"] = []
#   customer: "Customer"
# และบางครั้งต้องเรียก Order.model_rebuild() ตามทีหลัง`;

export const forwardRefTs = `// TypeScript — ลำดับการประกาศไม่เคยมีผล
interface Comment {
  text: string;
  replies: Comment[];     // อ้างถึงตัวเอง
}

interface Order {
  customer: Customer;     // ประกาศอยู่ข้างล่าง
}

interface Customer {
  name: string;
}`;

<TsGo ts={forwardRefTs} go={forwardRefPy} />

ถ้าคุณต้องอ่าน annotation เองแทนที่จะปล่อยให้ Pydantic จัดการ module `annotationlib` (ใหม่ใน 3.14 เช่นกัน) ให้คุณดึง annotation ออกมาได้หลายรูปแบบ รวมถึงรูปแบบ string สำหรับเครื่องมือที่ต้องไม่ทำให้เกิดการประเมินค่า

## ลองเล่น — stdlib dataclasses เป็น runnable stand-in

Pydantic ไม่สามารถรันใน browser ได้ `dataclasses` จาก standard library ให้รูปร่างเดียวกัน (typed fields, defaults, auto `__init__`) โดยไม่มี validation เพื่อให้สำรวจแนวคิดได้:

export const dataclassCode = `from dataclasses import dataclass, field, asdict
import json

@dataclass
class CreateItem:
    name: str
    price: float
    in_stock: bool = True
    tags: list[str] = field(default_factory=list)
    description: str | None = None

# สร้าง instance — รู้สึกเหมือน BaseModel(...)
item = CreateItem(name="Widget", price=9.99, tags=["sale", "new"])
print("item:", item)

# Serialize เป็น dict (เหมือน .model_dump())
data = asdict(item)
print("dict :", data)

# Serialize เป็น JSON
print("json :", json.dumps(data, indent=2))

# Nested dataclass
@dataclass
class OrderLine:
    item: CreateItem
    quantity: int

line = OrderLine(item=item, quantity=3)
print("\\nnested:", asdict(line))
`;

<Playground code={dataclassCode} />

export const quizPydantic = [
  {
    q: 'Pydantic equivalent ของ Zod schema หรือ TypeScript interface สำหรับ request body คืออะไร?',
    options: [
      'class ที่ inherit จาก dict',
      'TypedDict subclass',
      'class ที่ inherit จาก BaseModel',
      '@dataclass ที่มี validators',
    ],
    answer: 2,
  },
  {
    q: 'ใน Pydantic v2 method ใด serialize model instance เป็น Python dict?',
    options: ['.to_dict()', '.dict()', '.model_dump()', '.serialize()'],
    answer: 2,
  },
  {
    q: 'FastAPI รู้ได้อย่างไรว่าควร parse request body เป็น Pydantic model แทนที่จะถือว่าเป็น query param?',
    options: [
      'ตกแต่ง parameter ด้วย @Body()',
      'FastAPI ตรวจสอบว่า type hint เป็น BaseModel subclass',
      'ลงทะเบียนใน route decorator',
      'ต้องใช้ Body() จาก fastapi เสมอ',
    ],
    answer: 1,
  },
  {
    q: 'ใน Python 3.14 จะ annotate field ของ Pydantic ที่ type ถูกนิยามทีหลังในไฟล์อย่างไร?',
    options: [
      'ใส่ quote ชื่อ type แล้วเรียก model_rebuild()',
      'เขียนชื่อ type ตรง ๆ เพราะ annotation ถูกประเมินแบบ lazy',
      'เพิ่ม from __future__ import annotations ไว้บนสุด',
      'ย้าย class definition ไปไว้เหนือจุดที่อ้างถึง',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="api-fastapi/pydantic-models" questions={quizPydantic} />

<ProgressTracker client:visible id="api-fastapi/pydantic-models" />
