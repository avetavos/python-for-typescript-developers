---
title: การเชื่อมต่อฐานข้อมูล
description: เชื่อม database connection เข้า Echo handler ด้วย database/sql หรือ ORM — เปรียบกับ TypeORM, Prisma หรือ Drizzle ใน Node
---

import TsGo from '../../../../components/TsGo.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## ORM landscape ของ Node.js เทียบกับ Go

นักพัฒนา TypeScript คุ้นเคยกับการเลือกระหว่าง Prisma, TypeORM, Drizzle, Knex หรือ Sequelize Go มีตัวเลือกที่คล้ายกัน — GORM, sqlc, Bun, Ent — แต่ package `database/sql` ของ stdlib เป็นตัวเลือกชั้นหนึ่งที่ควรรู้จัก

| สิ่งที่ต้องการ | ตัวเลือกใน Node.js | ตัวเลือกใน Go |
|---|---|---|
| Low-level | `pg`, `mysql2` (raw driver) | `database/sql` + driver (เช่น `lib/pq`, `pgx`) |
| Query builder | `knex` | `squirrel`, raw `database/sql` |
| Full ORM | `TypeORM`, `Drizzle`, `Sequelize` | `GORM`, `ent`, `bun` |
| Codegen จาก SQL | — | `sqlc` (SQL → type-safe Go) |

บทนี้ใช้ `database/sql` กับ driver `lib/pq` สำหรับ PostgreSQL — เป็น baseline ที่ถ่ายโอนความรู้ได้มากที่สุด

## เปิด connection pool

export const tsDbConnect = `// Node.js — pg connection pool
import { Pool } from 'pg';

const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
});

export default pool;`;

export const goDbConnect = `// Go — database/sql + lib/pq
import (
    "database/sql"
    "log"
    "os"

    _ "github.com/lib/pq" // ลงทะเบียน driver ผ่าน side-effect import
)

func NewDB() *sql.DB {
    dsn := os.Getenv("DATABASE_URL")
    db, err := sql.Open("postgres", dsn)
    if err != nil {
        log.Fatalf("sql.Open: %v", err)
    }
    if err := db.Ping(); err != nil {
        log.Fatalf("db.Ping: %v", err)
    }
    db.SetMaxOpenConns(25)
    db.SetMaxIdleConns(5)
    return db
}`;

<TsGo client:visible ts={tsDbConnect} go={goDbConnect} />

## Store layer — repository pattern

แทนที่จะเรียก `db` โดยตรงจาก handler ห่อ database logic ไว้ใน store struct ซึ่งเหมือน Repository pattern ใน NestJS และทำให้ handler เบาลง

export const tsRepo = `// NestJS — repository injection
@Injectable()
export class BooksRepository {
  constructor(
    @InjectRepository(Book)
    private readonly repo: Repository<Book>,
  ) {}

  async findById(id: string): Promise<Book | null> {
    return this.repo.findOneBy({ id });
  }
}`;

export const goStore = `// Go — store struct (manual DI)
type BookStore struct {
    db *sql.DB
}

func NewBookStore(db *sql.DB) *BookStore {
    return &BookStore{db: db}
}

func (s *BookStore) FindByID(ctx context.Context, id string) (Book, error) {
    const q = \`SELECT id, title, author, year FROM books WHERE id = $1\`
    var b Book
    err := s.db.QueryRowContext(ctx, q, id).Scan(
        &b.ID, &b.Title, &b.Author, &b.Year,
    )
    if errors.Is(err, sql.ErrNoRows) {
        return Book{}, fmt.Errorf("FindByID: %w", ErrNotFound)
    }
    if err != nil {
        return Book{}, fmt.Errorf("FindByID: %w", err)
    }
    return b, nil
}`;

<TsGo client:visible ts={tsRepo} go={goStore} />

## Insert row

ใช้ parameterized query (`$1`, `$2`, …) เสมอ ห้าม interpolate value เข้าไปใน SQL string โดยตรง — นั่นคือช่องโหว่ SQL injection

export const tsInsert = `// Prisma insert
const book = await prisma.book.create({
  data: { title, author, year },
});`;

export const goInsert = `// database/sql insert — parameterized query
func (s *BookStore) Insert(ctx context.Context, req CreateBookRequest) (Book, error) {
    const q = \`
        INSERT INTO books (title, author, year)
        VALUES ($1, $2, $3)
        RETURNING id, title, author, year
    \`
    var b Book
    err := s.db.QueryRowContext(ctx, q,
        req.Title, req.Author, req.Year,
    ).Scan(&b.ID, &b.Title, &b.Author, &b.Year)
    if err != nil {
        return Book{}, fmt.Errorf("Insert: %w", err)
    }
    return b, nil
}`;

<TsGo client:visible ts={tsInsert} go={goInsert} />

## เชื่อม store เข้า handler ผ่าน main.go

```go
// main.go
db := NewDB()

bookStore   := store.NewBookStore(db)
bookHandler := handler.NewBookHandler(bookStore)

api := e.Group("/api")
api.GET("/books/:id", bookHandler.Get)
api.POST("/books",    bookHandler.Create)
```

ไม่ต้องการ DI container — Go's type system และ constructor function จัดการได้อย่างชัดเจน

## ตัวเลือกระดับสูงอื่น ๆ

- **sqlc** — เขียน SQL file ธรรมดา รัน `sqlc generate` ได้ฟังก์ชัน Go ที่ type-safe ไม่มี reflection overhead เหมาะกับ team ที่ต้องการควบคุม SQL
- **GORM** — ORM แบบ ActiveRecord คุ้นเคยถ้ามาจาก Sequelize แต่เสียความโปร่งใสของ query บ้าง
- **pgx/pgxpool** — driver PostgreSQL ประสิทธิภาพสูง (เหนือกว่า `database/sql`) แนะนำสำหรับ production pattern `QueryRowContext` / `Scan` เหมือนกัน แต่ throughput ดีกว่า

<GoOnly title="ส่ง context ให้ทุก DB call เสมอ">

ทุกเมธอดใน `database/sql` มีตัวแปร Context: `QueryContext`, `QueryRowContext`, `ExecContext` **ใช้ตัวแปร context เสมอ** และส่ง `c.Request().Context()` จาก handler ทำให้ database query ถูกยกเลิกได้หากเกิด client disconnect หรือ deadline หมดอายุ — ป้องกันงานเสียเปล่าใน connection pool

```go
// ดี
rows, err := s.db.QueryContext(ctx, query, args...)

// ไม่ดี — ไม่รับรู้การยกเลิก
rows, err := s.db.Query(query, args...)
```

</GoOnly>

> รันบนเครื่องของคุณ — ต้องการ PostgreSQL จริง, driver module `lib/pq` และ network port

export const quizQuestionsDatabase = [
  {
    q: 'ทำไมต้องใช้ parameterized query ($1, $2) แทน string interpolation ใน SQL?',
    options: [
      'Parameterized query เร็วกว่าในทุกกรณี',
      'String interpolation เป็น syntax ที่ไม่ valid ใน Go',
      'Parameterized query ป้องกัน SQL injection',
      'database/sql รองรับเฉพาะ parameterized query',
    ],
    answer: 2,
  },
  {
    q: 'sql.QueryRowContext คืน error ใดเมื่อไม่มีแถวที่ตรงกัน?',
    options: ['nil', 'io.EOF', 'sql.ErrNoRows', 'sql.ErrEmpty'],
    answer: 2,
  },
  {
    q: 'ทำไมต้องใช้ตัวแปร Context ของ database/sql (เช่น QueryContext)?',
    options: [
      'ตัวที่ไม่มี context ถูก deprecated',
      'เพื่ออนุญาต cancellation และ timeout propagation จาก HTTP request',
      'Context variant คืนผลลัพธ์ที่ typed',
      'เฉพาะ context variant เท่านั้นที่รองรับ parameterized query',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="api-echo/database" questions={quizQuestionsDatabase} />

<ProgressTracker client:visible id="api-echo/database" />
