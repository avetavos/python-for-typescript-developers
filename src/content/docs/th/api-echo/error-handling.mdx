---
title: การจัดการ Error
description: คืน error จาก handler, ใช้ echo.HTTPError สำหรับ HTTP-aware error และเขียน central error handler — เชื่อมกับ philosophy errors-as-values ของ Go
---

import TsGo from '../../../../components/TsGo.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## Error as value — philosophy ของ Go

TypeScript ใช้ `throw` และ `try/catch` Go ถือว่า error เป็น return value ธรรมดา: ทุกฟังก์ชันที่อาจ fail จะคืน `(result, error)` คุณตรวจสอบ error อย่างชัดเจน — compiler ไม่ให้คุณเพิกเฉยโดยไม่รู้ตัว Echo ต่อยอดนี้ตามธรรมชาติ: handler ที่คืน error ที่ไม่เป็น nil จะ trigger error handler ของ Echo

export const tsErrorThrow = `// TypeScript / Express — throw-based
async function getBook(id: string): Promise<Book> {
  const book = await db.findOne(id);
  if (!book) throw new NotFoundException('book not found');
  return book;
}

// Express error middleware ท้ายสุด
app.use((err, req, res, next) => {
  const status = err.status ?? 500;
  res.status(status).json({ message: err.message });
});`;

export const goErrorReturn = `// Go / Echo — return-based
func findBook(ctx context.Context, id string) (Book, error) {
    book, err := store.FindByID(ctx, id)
    if err != nil {
        return Book{}, fmt.Errorf("findBook: %w", err)
    }
    return book, nil
}

// Handler — ส่งต่อ error ไปยัง error handler ของ Echo
func getBook(c echo.Context) error {
    book, err := findBook(c.Request().Context(), c.Param("id"))
    if err != nil {
        return echo.NewHTTPError(http.StatusNotFound, err.Error())
    }
    return c.JSON(http.StatusOK, book)
}`;

<TsGo client:visible ts={tsErrorThrow} go={goErrorReturn} />

## echo.NewHTTPError — error ที่รู้จัก HTTP

`echo.NewHTTPError(statusCode, message)` สร้าง `*echo.HTTPError` เมื่อ handler คืนค่านี้ default error handler ของ Echo จะ serialize เป็น `{"message": "..."}` พร้อม HTTP status code ที่ถูกต้อง สำหรับ `error` ธรรมดา Echo จะคืน `500 Internal Server Error`

```go
// 400
return echo.NewHTTPError(http.StatusBadRequest, "invalid id format")

// 401
return echo.NewHTTPError(http.StatusUnauthorized, "missing token")

// 404
return echo.NewHTTPError(http.StatusNotFound, "book not found")

// 409
return echo.NewHTTPError(http.StatusConflict, "book already exists")
```

## Wrapping error พร้อม context

ห่อ error ด้วย `fmt.Errorf("context: %w", err)` เสมอ เพื่อให้ error ต้นฉบับถูกเก็บไว้สำหรับการตรวจสอบด้วย `errors.Is` / `errors.As` และสำหรับ structured logging

export const tsErrorWrap = `// TypeScript — wrapping with cause
throw new Error('failed to save book', { cause: originalError });

// NestJS — HTTP-specific
throw new InternalServerErrorException('failed to save book');`;

export const goErrorWrap = `// Go — wrapping ด้วย %w
func (s *BookService) Create(ctx context.Context, req CreateBookRequest) (Book, error) {
    book, err := s.store.Insert(ctx, req)
    if err != nil {
        // %w เก็บ error ต้นฉบับไว้สำหรับ errors.Is / errors.As
        return Book{}, fmt.Errorf("BookService.Create: %w", err)
    }
    return book, nil
}

// Handler — ตรวจสอบชนิด error
func createBook(c echo.Context) error {
    book, err := svc.Create(c.Request().Context(), req)
    if err != nil {
        // errors.Is เดิน wrap chain
        if errors.Is(err, store.ErrDuplicate) {
            return echo.NewHTTPError(http.StatusConflict, "book already exists")
        }
        return echo.NewHTTPError(http.StatusInternalServerError, "failed to create book")
    }
    return c.JSON(http.StatusCreated, book)
}`;

<TsGo client:visible ts={tsErrorWrap} go={goErrorWrap} />

## Central error handler

Echo ให้คุณแทน default error handler ด้วย `e.HTTPErrorHandler` นี่คือที่ที่คุณ log error, แปลง domain error เป็น HTTP code และทำให้ response envelope สม่ำเสมอ

export const tsErrMiddleware = `// Express — error middleware (4 argument, ต้องอยู่ท้ายสุด)
app.use((err: Error, req: Request, res: Response, next: NextFunction) => {
  console.error(err);
  if (err instanceof HttpException) {
    return res.status(err.status).json({ message: err.message });
  }
  res.status(500).json({ message: 'internal server error' });
});`;

export const goErrHandler = `// Echo — custom HTTP error handler
func customErrorHandler(err error, c echo.Context) {
    var he *echo.HTTPError
    if errors.As(err, &he) {
        // Echo HTTPError — ใช้ status code ของมัน
        _ = c.JSON(he.Code, map[string]any{
            "message": he.Message,
        })
        return
    }

    // Error ที่ไม่รู้จัก — log และคืน 500
    c.Logger().Errorf("unhandled error: %v", err)
    _ = c.JSON(http.StatusInternalServerError, map[string]any{
        "message": "internal server error",
    })
}

// ลงทะเบียนใน main():
e.HTTPErrorHandler = customErrorHandler`;

<TsGo client:visible ts={tsErrMiddleware} go={goErrHandler} />

<GoOnly title="Sentinel error — เทียบเท่า custom exception ของ Go">

กำหนด sentinel error ระดับ package เพื่อแทน failure mode ที่รู้จัก Handler และ service สามารถตรวจสอบด้วย `errors.Is`:

```go
// store/errors.go
var (
    ErrNotFound  = errors.New("not found")
    ErrDuplicate = errors.New("duplicate entry")
)

// store/books.go — wrap sentinel เมื่อเกิด DB error
if pqErr, ok := err.(*pq.Error); ok && pqErr.Code == "23505" {
    return Book{}, fmt.Errorf("Insert: %w", store.ErrDuplicate)
}

// handler — แปลง domain error เป็น HTTP code
if errors.Is(err, store.ErrNotFound) {
    return echo.NewHTTPError(http.StatusNotFound, "book not found")
}
```

วิธีนี้ทำให้ HTTP semantic อยู่ที่ handler layer และ business logic อยู่ที่ service layer อย่างชัดเจน

</GoOnly>

> รันบนเครื่องของคุณ — ต้องการ Echo module และ network port

export const quizQuestionsError = [
  {
    q: 'เกิดอะไรขึ้นเมื่อ Echo handler คืน bare error (ที่ไม่ใช่ HTTPError)?',
    options: [
      'Error ถูกละเว้นอย่างเงียบ ๆ',
      'Echo คืน 500 Internal Server Error',
      'Echo คืน 400 Bad Request',
      'Echo panic',
    ],
    answer: 1,
  },
  {
    q: 'format string verb ใดที่เก็บ error ต้นฉบับไว้สำหรับการตรวจสอบด้วย errors.Is?',
    options: ['%v', '%s', '%w', '%e'],
    answer: 2,
  },
  {
    q: 'ลงทะเบียน custom central error handler ใน Echo อย่างไร?',
    options: [
      'e.Use(errorHandler)',
      'e.OnError = errorHandler',
      'e.HTTPErrorHandler = errorHandler',
      'e.ErrorHandler(errorHandler)',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="api-echo/error-handling" questions={quizQuestionsError} />

<ProgressTracker client:visible id="api-echo/error-handling" />
