---
title: Context — การยกเลิกและ Deadlines
description: ใช้ context.Context สำหรับ cancellation, deadlines และ timeouts; เปรียบเทียบกับ AbortController ของ JavaScript
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## AbortController vs context.Context

ใน TypeScript/Web API คุณใช้ `AbortController` เพื่อยกเลิก fetch requests:

```typescript
const controller = new AbortController();
setTimeout(() => controller.abort(), 5000); // timeout 5 วินาที
const res = await fetch(url, { signal: controller.signal });
```

ใน Go `context.Context` เป็น mechanism สำคัญสำหรับ cancellation, deadlines และ timeouts ที่ส่งต่อผ่าน call chain ทั้งหมด

## context.WithCancel

`context.WithCancel` สร้าง context ที่ยกเลิกได้ด้วยการเรียก cancel function:

export const tsAbortCode = `// TypeScript: AbortController
const controller = new AbortController();
const { signal } = controller;

async function worker(signal: AbortSignal) {
  try {
    await doWork(signal);
  } catch (e) {
    if (e.name === 'AbortError') {
      console.log('Worker cancelled');
    }
  }
}

controller.abort(); // ยกเลิก`;

export const goWithCancelCode = `// Go: context.WithCancel
package main

import (
    "context"
    "fmt"
    "sync"
)

func worker(ctx context.Context, id int, wg *sync.WaitGroup) {
    defer wg.Done()
    select {
    case <-ctx.Done(): // รอการยกเลิก
        fmt.Printf("Worker %d cancelled: %v\\n", id, ctx.Err())
    }
}

func main() {
    ctx, cancel := context.WithCancel(context.Background())
    var wg sync.WaitGroup

    for i := 1; i <= 3; i++ {
        wg.Add(1)
        go worker(ctx, i, &wg)
    }

    cancel()   // ยกเลิก workers ทั้งหมด
    wg.Wait()
    fmt.Println("All workers stopped")
}`;

<TsGo client:visible ts={tsAbortCode} go={goWithCancelCode} />

## context.WithTimeout

สำหรับ operations ที่มี deadline:

export const tsTimeoutCode = `// TypeScript: timeout ด้วย Promise.race
async function withTimeout<T>(
  fn: () => Promise<T>,
  ms: number
): Promise<T> {
  const timeout = new Promise<never>((_, reject) =>
    setTimeout(() => reject(new Error(\`Timeout after \${ms}ms\`)), ms)
  );
  return Promise.race([fn(), timeout]);
}`;

export const goWithTimeoutCode = `// Go: context.WithTimeout
package main

import (
    "context"
    "fmt"
    "sync"
    "time"
)

func slowOperation(ctx context.Context, wg *sync.WaitGroup) {
    defer wg.Done()
    select {
    case <-time.After(5 * time.Second): // จำลอง slow operation
        fmt.Println("Operation complete")
    case <-ctx.Done():
        fmt.Println("Operation timed out:", ctx.Err())
    }
}

func main() {
    // timeout หลัง 100ms
    ctx, cancel := context.WithTimeout(
        context.Background(),
        100*time.Millisecond,
    )
    defer cancel() // ดีที่สุดคือ defer cancel() เสมอ

    var wg sync.WaitGroup
    wg.Add(1)
    go slowOperation(ctx, &wg)
    wg.Wait()
}`;

<TsGo client:visible ts={tsTimeoutCode} go={goWithTimeoutCode} />

## Context Values — ส่งข้อมูลผ่าน Call Chain

export const tsReqCode = `// TypeScript: ส่ง request-scoped data ผ่าน function args
async function handler(req: Request) {
  const userId = req.headers.get('x-user-id');
  await processRequest(userId);
}`;

export const goCtxValueCode = `// Go: context.WithValue สำหรับ request-scoped data
package main

import (
    "context"
    "fmt"
)

type contextKey string

const userIDKey contextKey = "userID"

func processRequest(ctx context.Context) {
    userID := ctx.Value(userIDKey)
    fmt.Println("Processing for user:", userID)
}

func main() {
    ctx := context.WithValue(
        context.Background(),
        userIDKey,
        "user-123",
    )
    processRequest(ctx)
}`;

<TsGo client:visible ts={tsReqCode} go={goCtxValueCode} />

<GoOnly title="กฎการใช้ Context ใน Go">

1. **Context เป็น argument แรกเสมอ** — `func Do(ctx context.Context, ...)`
2. **อย่าเก็บ context ใน struct** — ส่งผ่าน function arguments
3. **defer cancel() เสมอ** — ป้องกัน resource leak แม้ context จะ cancel ก่อน
4. **ใช้ context.Background()** ที่ top level; ใช้ context.TODO() เมื่อยังไม่แน่ใจ
5. **context.WithValue ใช้สำหรับ request-scoped data เท่านั้น** ไม่ใช่สำหรับ optional parameters

</GoOnly>

export const playgroundContextCode = `package main

import (
	"context"
	"fmt"
	"sync"
)

func worker(ctx context.Context, id int, wg *sync.WaitGroup) {
	defer wg.Done()
	select {
	case <-ctx.Done():
		fmt.Printf("Worker %d stopped: %v\\n", id, ctx.Err())
	}
}

func main() {
	ctx, cancel := context.WithCancel(context.Background())
	var wg sync.WaitGroup

	for i := 1; i <= 3; i++ {
		wg.Add(1)
		go worker(ctx, i, &wg)
	}

	cancel()
	wg.Wait()
	fmt.Println("All workers stopped")
}`;

<Playground client:visible code={playgroundContextCode} />

export const quizQuestionsContext = [
  { q: 'context.Context เปรียบเทียบกับอะไรใน JavaScript?', options: ['Promise', 'AbortController', 'EventEmitter', 'setTimeout'], answer: 1 },
  { q: 'ทำไมถึงควร defer cancel() เสมอ?', options: ['เพื่อความเร็ว', 'ป้องกัน resource leak', 'เพื่อ print log', 'เพื่อ restart goroutines'], answer: 1 },
  { q: 'Context ควรถูก pass อย่างไร?', options: ['เก็บใน global variable', 'เก็บใน struct field', 'เป็น argument แรกของ function', 'ผ่าน channel'], answer: 2 },
  { q: 'channel ใดที่ close เมื่อ context ถูก cancel?', options: ['ctx.Cancel()', 'ctx.Stop()', 'ctx.Done()', 'ctx.End()'], answer: 2 },
];

<Quiz client:visible id="concurrency/context" questions={quizQuestionsContext} />

<ProgressTracker client:visible id="concurrency/context" />
