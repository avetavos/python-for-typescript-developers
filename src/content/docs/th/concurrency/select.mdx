---
title: Select
description: เรียนรู้ select statement สำหรับเลือกจากหลาย channels; เปรียบเทียบกับ Promise.race
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## Promise.race vs Select

ใน TypeScript `Promise.race` รับ Promise หลายตัวและ resolve ด้วย Promise แรกที่เสร็จ:

```typescript
const result = await Promise.race([
  fetch('/api/primary'),
  fetch('/api/backup'),
]);
```

ใน Go `select` statement ทำสิ่งเดียวกันกับ channels — รอฟัง channel หลายตัว และดำเนินการตาม channel แรกที่พร้อม:

```go
select {
case msg := <-ch1:
    fmt.Println("from ch1:", msg)
case msg := <-ch2:
    fmt.Println("from ch2:", msg)
}
```

## Select Statement พื้นฐาน

export const tsRaceCode = `// TypeScript: Promise.race
async function withTimeout<T>(
  promise: Promise<T>,
  ms: number
): Promise<T> {
  const timeout = new Promise<never>((_, reject) =>
    setTimeout(() => reject(new Error("timeout")), ms)
  );
  return Promise.race([promise, timeout]);
}`;

export const goSelectCode = `// Go: select สำหรับเลือก channel แรกที่พร้อม
package main

import "fmt"

func main() {
    ch1 := make(chan string, 1)
    ch2 := make(chan string, 1)

    ch1 <- "one"
    ch2 <- "two"

    // รับจาก ch1 ก่อน (deterministic เพราะใช้ select แยก)
    select {
    case msg := <-ch1:
        fmt.Println("Received from ch1:", msg)
    }
    select {
    case msg := <-ch2:
        fmt.Println("Received from ch2:", msg)
    }
}`;

<TsGo client:visible ts={tsRaceCode} go={goSelectCode} />

## Default Case — Non-blocking Select

เพิ่ม `default` เพื่อให้ select ไม่ block เมื่อไม่มี channel พร้อม:

export const tsNonBlockCode = `// TypeScript: non-blocking check ด้วย Promise
async function tryReceive(): Promise<string | null> {
  const result = await Promise.race([
    getMessage(),
    Promise.resolve(null), // fallback ทันที
  ]);
  return result;
}`;

export const goDefaultSelectCode = `// Go: non-blocking select ด้วย default
package main

import "fmt"

func main() {
    ch := make(chan string, 1)

    select {
    case msg := <-ch:
        fmt.Println("Got:", msg)
    default:
        fmt.Println("No message available") // รันทันที
    }

    ch <- "hello"

    select {
    case msg := <-ch:
        fmt.Println("Got:", msg)
    default:
        fmt.Println("No message available")
    }
}`;

<TsGo client:visible ts={tsNonBlockCode} go={goDefaultSelectCode} />

## Select กับ Context Cancellation

Pattern ที่พบบ่อยมากใน Go คือการใช้ select เพื่อฟัง channel งาน และ context cancellation พร้อมกัน:

export const tsAbortCode = `// TypeScript: AbortController สำหรับ cancellation
async function processWithAbort(signal: AbortSignal) {
  for (const item of items) {
    if (signal.aborted) break;
    await processItem(item);
  }
}`;

export const goCtxSelectCode = `// Go: select กับ ctx.Done() สำหรับ cancellation
package main

import (
    "context"
    "fmt"
    "sync"
)

func worker(ctx context.Context, jobs <-chan int, wg *sync.WaitGroup) {
    defer wg.Done()
    for {
        select {
        case j, ok := <-jobs:
            if !ok {
                return // channel ถูก close
            }
            fmt.Println("Processing job:", j)
        case <-ctx.Done():
            fmt.Println("Worker cancelled")
            return
        }
    }
}`;

<TsGo client:visible ts={tsAbortCode} go={goCtxSelectCode} />

<GoOnly title="Select เลือก Case แบบสุ่มเมื่อหลาย Cases พร้อม">

เมื่อหลาย cases ใน select พร้อมพร้อมกัน Go จะ**เลือกแบบสุ่ม** (pseudo-random) ไม่ใช่ตามลำดับที่เขียน

```go
select {
case msg := <-ch1: // อาจถูกเลือกหรือไม่ก็ได้
    fmt.Println(msg)
case msg := <-ch2: // อาจถูกเลือกหรือไม่ก็ได้
    fmt.Println(msg)
}
```

นี่คือเหตุผลที่ตัวอย่าง playground ใช้ select แยกกันสองตัว เพื่อให้ output deterministic สำหรับการเรียน

</GoOnly>

export const playgroundSelectCode = `package main

import "fmt"

func main() {
	ch1 := make(chan string, 1)
	ch2 := make(chan string, 1)

	ch1 <- "hello"
	ch2 <- "world"

	// รับจาก ch1 ก่อน
	select {
	case msg := <-ch1:
		fmt.Println("ch1:", msg)
	}

	// รับจาก ch2
	select {
	case msg := <-ch2:
		fmt.Println("ch2:", msg)
	}

	// default case: non-blocking
	select {
	case msg := <-ch1:
		fmt.Println("extra:", msg)
	default:
		fmt.Println("no more messages")
	}
}`;

<Playground client:visible code={playgroundSelectCode} />

export const quizQuestionsSelect = [
  { q: 'select statement ใน Go เปรียบเทียบกับอะไรใน JavaScript?', options: ['switch/case', 'Promise.all', 'Promise.race', 'async/await'], answer: 2 },
  { q: 'เมื่อหลาย cases พร้อมพร้อมกัน select จะทำอย่างไร?', options: ['เลือก case แรกเสมอ', 'เลือก case สุดท้าย', 'เลือกแบบสุ่ม', 'throw error'], answer: 2 },
  { q: 'default case ใน select ทำให้เกิดอะไร?', options: ['Block จนมี channel พร้อม', 'ทำให้ select non-blocking', 'Panic ถ้าไม่มี channel พร้อม', 'รอ 1 วินาทีแล้ว execute'], answer: 1 },
];

<Quiz client:visible id="concurrency/select" questions={quizQuestionsSelect} />

<ProgressTracker client:visible id="concurrency/select" />
