---
title: Reflection
description: reflect package, struct tags ที่ runtime, use cases และข้อควรระวัง — เปรียบเทียบกับ TypeScript's reflect-metadata และ decorators
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## Runtime types: TypeScript vs Go

ใน TypeScript คุณตรวจสอบ type metadata ที่ runtime ได้ผ่าน `reflect-metadata` และ decorators — แต่นี่เป็น opt-in, ต้องใช้ `experimentalDecorators` และ `emitDecoratorMetadata` ใน `tsconfig.json`, และ metadata ถูก attach ด้วยตนเองโดย decorator functions JavaScript ลบข้อมูล type เกือบทั้งหมดที่ runtime; decorators คือ workaround ไม่ใช่ language primitive

Go ถูก compile แต่ไม่เหมือน TypeScript มันเก็บข้อมูล type เต็มรูปแบบที่ runtime ผ่าน `reflect` package ค่าทุกค่าใน Go รู้ type ของตัวเองที่ runtime — ไม่ต้อง annotations, ไม่ต้อง build flags

export const tsReflectCode = `// TypeScript — ต้องใช้ reflect-metadata + decorator compiler flags
import 'reflect-metadata';

function Column(target: any, key: string) {
  const type = Reflect.getMetadata('design:type', target, key);
  console.log(\`\${key}: \${type.name}\`);
}

class User {
  @Column name: string = '';
  @Column age: number = 0;
}`;

export const goReflectCode = `// Go — built-in ไม่ต้องใช้ flags
import "reflect"

type User struct {
    Name string
    Age  int
}

u := User{Name: "Alice", Age: 30}
t := reflect.TypeOf(u)

for i := 0; i < t.NumField(); i++ {
    f := t.Field(i)
    fmt.Printf("%s: %s\\n", f.Name, f.Type)
}
// Name: string
// Age: int`;

<TsGo client:visible ts={tsReflectCode} go={goReflectCode} />

## Struct tags

Struct tags คือ string literals ที่ฝังอยู่ในการประกาศ field พวกมันมองไม่เห็นที่ compile time และอ่านได้เฉพาะที่ runtime ผ่าน `reflect` นี่คือวิธีที่ `encoding/json`, `database/sql` scanners และไลบรารี validation เช่น `go-playground/validator` รู้ว่า JSON key ใด map กับ field ใด

export const tsTagCode = `// TypeScript — class-validator ใช้ decorators
import { IsEmail, IsString, Min } from 'class-validator';

class CreateUserDto {
  @IsString()
  name: string;

  @IsEmail()
  email: string;

  @Min(0)
  age: number;
}`;

export const goTagCode = `// Go — struct tags (plain strings อ่านผ่าน reflect)
type CreateUserDTO struct {
    Name  string \`json:"name"  validate:"required"\`
    Email string \`json:"email" validate:"required,email"\`
    Age   int    \`json:"age"   validate:"min=0"\`
}

// encoding/json ใช้ "json" tag อัตโนมัติ
// Validator libraries อ่าน "validate" ที่ runtime`;

<TsGo client:visible ts={tsTagCode} go={goTagCode} />

## อ่าน struct tags ที่ runtime

pattern canonical สำหรับอ่าน tags คือ `reflect.Type.Field(i).Tag.Get("key")`:

```go
func printJSONTags(v any) {
    t := reflect.TypeOf(v)
    if t.Kind() == reflect.Ptr {
        t = t.Elem() // dereference pointer
    }
    for i := 0; i < t.NumField(); i++ {
        f := t.Field(i)
        jsonKey := f.Tag.Get("json")
        fmt.Printf("Field: %-10s  json=%q\n", f.Name, jsonKey)
    }
}
```

## reflect.Value vs reflect.Type

`reflect.TypeOf` ให้ **type descriptor** (kind, fields, methods) `reflect.ValueOf` ให้ **boxed value** ที่คุณตรวจสอบและ — อย่างระมัดระวัง — เปลี่ยนแปลงได้

```go
u := User{Name: "Alice", Age: 30}

// Type introspection
t := reflect.TypeOf(u)
fmt.Println(t.Kind())       // struct
fmt.Println(t.NumField())   // 2

// Value introspection
v := reflect.ValueOf(u)
fmt.Println(v.Field(0))     // Alice
fmt.Println(v.Field(1))     // 30

// Mutation ต้องใช้ pointer และ CanSet()
vp := reflect.ValueOf(&u).Elem()
vp.Field(0).SetString("Bob")
fmt.Println(u.Name) // Bob
```

<GoOnly title="เมื่อใดไม่ควรใช้ reflect">

Reflection ใน Go ทรงพลังแต่มีต้นทุนจริง:

- **Performance:** การเรียก `reflect` ช้ากว่าการเข้าถึง field โดยตรงมาก อย่าใช้ reflection ใน hot loops
- **Type safety สูญหายที่ boundary:** โค้ดที่ใช้ `reflect.Value.Set*` ข้าม compiler Panics แทนที่ compile errors
- **Readability:** โค้ดที่ใช้ reflection หนักกว่าอ่านและ refactor

**ใช้ reflection เมื่อ:**
- เขียนโค้ดระดับ framework (ORMs, JSON codecs, validators, dependency injection)
- Implement generic serializers ที่ต้องจัดการ struct shapes ที่ไม่รู้จักที่ runtime

**อย่าใช้ reflection เมื่อ:**
- คุณรู้ types ที่ compile time — ใช้ generics หรือ interfaces แทน
- ฟังก์ชันถูกเรียกใน tight loop — benchmark ก่อน

</GoOnly>

## ลองดูเลย

export const playgroundReflectCode = `package main

import (
	"fmt"
	"reflect"
)

type User struct {
	Name  string \`json:"name"  validate:"required"\`
	Email string \`json:"email" validate:"required,email"\`
	Age   int    \`json:"age"\`
}

func printStructTags(v any) {
	t := reflect.TypeOf(v)
	if t.Kind() == reflect.Ptr {
		t = t.Elem()
	}
	fmt.Println("--- Struct tags ---")
	for i := 0; i < t.NumField(); i++ {
		f := t.Field(i)
		fmt.Printf("%-8s  json=%q  validate=%q\\n",
			f.Name,
			f.Tag.Get("json"),
			f.Tag.Get("validate"),
		)
	}
}

func printValues(v any) {
	rv := reflect.ValueOf(v)
	rt := rv.Type()
	fmt.Println("--- Values ---")
	for i := 0; i < rv.NumField(); i++ {
		fmt.Printf("%-8s = %v\\n", rt.Field(i).Name, rv.Field(i))
	}
}

func main() {
	u := User{Name: "Alice", Email: "alice@example.com", Age: 30}
	printStructTags(u)
	printValues(u)
}`;

<Playground client:visible code={playgroundReflectCode} />

export const quizQuestionsReflection = [
  { q: 'ฟังก์ชันใดที่ return runtime type descriptor ของค่า Go?', options: ['reflect.ValueOf', 'reflect.TypeOf', 'reflect.KindOf', 'reflect.MetaOf'], answer: 1 },
  { q: 'ต้องทำอะไรก่อนเรียก SetString() บน reflect.Value field?', options: ['Cast เป็น interface{}', 'Get pointer และเรียก .Elem()', 'เรียก reflect.New()', 'ไม่ต้องทำอะไร — ใช้ได้กับทุก value'], answer: 1 },
  { q: 'Go struct tags เก็บและอ่านจากที่ไหน?', options: ['ไฟล์ metadata แยกต่างหาก', 'Field declaration string ของ struct, อ่านผ่าน reflect ที่ runtime', 'Registry annotation ของ compiler', 'ไฟล์ go.mod'], answer: 1 },
  { q: 'เมื่อใดควรเลือก generics แทน reflect สำหรับ utility function ใหม่?', options: ['เมื่อ types ไม่รู้จักที่ compile time', 'เมื่อกำลังเขียน ORM', 'เมื่อ types รู้จักที่ compile time และต้องการความปลอดภัยของ type', 'เมื่อ performance ไม่สำคัญ'], answer: 2 },
];

<Quiz client:visible id="advanced/reflection" questions={quizQuestionsReflection} />

<ProgressTracker client:visible id="advanced/reflection" />
