---
title: Modules เชิงลึก
description: Semantic versioning, go.mod/go.sum, replace directives, vendoring และ Go workspaces — เปรียบเทียบกับ npm, package-lock.json และ npm workspaces
---

import TsGo from '../../../../components/TsGo.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## npm vs Go modules

ถ้าคุณรู้จัก npm คุณเข้าใจ Go modules 90% แล้ว ทั้งสองระบบใช้ semantic versioning, lock files และ manifest file mental model map ได้ชัดเจน — แต่กลไกแตกต่างกันในวิธีที่สำคัญ

export const npmManifestCode = `// package.json
{
  "name": "my-app",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}`;

export const goManifestCode = `// go.mod
module github.com/myorg/my-app

go 1.21

require (
    github.com/gin-gonic/gin v1.9.1
    github.com/stretchr/testify v1.8.4
)

// ไม่มี devDependencies แยกต่างหากใน Go
// deps ที่ใช้เฉพาะ test รวมอยู่ใน require;
// compiler ไม่รวมมันใน production binaries`;

<TsGo client:visible ts={npmManifestCode} go={goManifestCode} />

## go.mod — manifest

`go.mod` เทียบเท่า `package.json` มี 3 fields ที่ต้องมี:

```
module github.com/myorg/my-app   ← module path (import prefix)
go 1.21                          ← minimum Go version
require (...)                    ← direct dependencies
```

ความแตกต่างหลักจาก `package.json`:
- module path คือ string คล้าย URL, มักตรงกับ VCS repo path
- ไม่มี `devDependencies` dependencies ทั้งหมดอยู่ใน `require` Go linker รวมเฉพาะ packages ที่ binary ที่กำลัง build นำเข้า
- syntax range `^` ไม่มี คุณ pin version ที่แน่นอน (เช่น `v1.9.1`)

## go.sum — lock file

`go.sum` เทียบเท่า `package-lock.json` บันทึก cryptographic hashes สำหรับทุก dependency version:

```
github.com/gin-gonic/gin v1.9.1 h1:4idEAncQnU5cB7BerypkHKpy7LtzLuviaCYD2h8zebo=
github.com/gin-gonic/gin v1.9.1/go.mod h1:hPrL7YrpYKXt5YId3A/Tnip5kqbEAP+KLuI3SUcPTeU=
```

สอง hash lines ต่อ dependency:
- `h1:...` — hash ของ module zip content
- `/go.mod h1:...` — hash ของเฉพาะ `go.mod` file

ทั้งสองต้อง match ทุกเครื่องและใน CI — `go mod verify` ตรวจสิ่งนี้ คุณ commit `go.sum` ไปยัง version control (เหมือนกับ `package-lock.json`)

## คำสั่ง module ทั่วไป

```bash
# เริ่มต้น module ใหม่
go mod init github.com/myorg/my-app

# เพิ่ม dependency (อัพเดท go.mod + go.sum)
go get github.com/gin-gonic/gin@v1.9.1

# Upgrade ไป latest patch/minor
go get github.com/gin-gonic/gin@latest

# ลบ dependencies ที่ไม่ใช้
go mod tidy

# ตรวจสอบ modules ที่ download ทั้งหมด match go.sum
go mod verify

# แสดง direct + indirect dependencies ทั้งหมด
go list -m all

# แสดง versions ที่มีของ module
go list -m -versions github.com/gin-gonic/gin
```

## Major version semantics

Go modules encode **major version ใน import path** สำหรับ v2+ นี่คือความแตกต่างที่น่าแปลกใจที่สุดจาก npm:

export const npmMajorCode = `// npm — ชื่อ package เดิม แค่ version ใหม่
// package.json
"dependencies": {
  "some-lib": "^3.0.0"  // v3, import เดิม
}
// import someLib from 'some-lib';  ← ไม่เปลี่ยน`;

export const goMajorCode = `// Go — v2+ เปลี่ยน import path
// go.mod
require github.com/some-lib/core v2.0.0+incompatible

// ถ้าไลบรารีมี /v2 module ที่เหมาะสม:
require github.com/some-lib/core/v2 v2.0.0

// imports ของคุณเปลี่ยน:
import "github.com/some-lib/core/v2"
// v1:  import "github.com/some-lib/core"
// v2:  import "github.com/some-lib/core/v2"`;

<TsGo client:visible ts={npmMajorCode} go={goMajorCode} />

## replace directive — local development

เมื่อกำลัง develop dependency ควบคู่ไปกับ main module `replace` map module path ไปยัง local directory — เทียบเท่า `npm link` หรือ path `file:` ใน `package.json`:

```go
// go.mod
require github.com/myorg/shared-lib v1.2.3

replace github.com/myorg/shared-lib => ../shared-lib
```

ลบ `replace` ก่อน commit ไปยัง production ใช้ `go mod tidy` หลังจากนั้นเพื่อ cleanup

## Vendoring

Vendoring copy dependencies ทั้งหมดไปยัง directory `vendor/` ภายใน repo — มีประโยชน์สำหรับ air-gapped builds หรือการรับประกัน reproducibility โดยไม่พึ่ง module proxy:

```bash
# สร้าง/อัพเดท vendor directory
go mod vendor

# Build โดยใช้เฉพาะ vendored deps (ไม่สนใจ module cache)
go build -mod=vendor ./...

# ตรวจสอบ vendor match go.sum
go mod verify
```

<GoOnly title="go.env และ GOFLAGS">

คุณตั้งค่า `GONOSUMCHECK`, `GONOSUMDB`, `GOPROXY` และ `GOFLAGS` ใน shell หรือผ่าน `go env -w`:

```bash
# ใช้ corporate module proxy
go env -w GOPROXY=https://proxy.corp.example.com,direct

# Disable sum database สำหรับ private repos
go env -w GONOSUMDB=github.com/myorg/*

# ใช้ vendor เสมอถ้ามี
go env -w GOFLAGS=-mod=vendor
```

</GoOnly>

## Go workspaces (`go work`)

Workspaces (เพิ่มใน Go 1.18) ให้คุณ develop multiple inter-dependent modules พร้อมกันโดยไม่ต้องใช้ `replace` directives — เทียบเท่า npm workspaces หรือ yarn workspaces:

export const npmWorkspaceCode = `// npm workspaces — package.json ที่ root
{
  "workspaces": [
    "packages/api",
    "packages/shared"
  ]
}`;

export const goWorkspaceCode = `// Go workspaces — go.work ที่ repo root
go 1.21

use (
    ./api        // module: github.com/myorg/api
    ./shared     // module: github.com/myorg/shared
)

// ตอนนี้ api import shared ได้โดยตรงจาก disk
// ไม่ต้องใช้ replace directive`;

<TsGo client:visible ts={npmWorkspaceCode} go={goWorkspaceCode} />

```bash
# เริ่มต้น workspace (สร้าง go.work)
go work init ./api ./shared

# เพิ่ม module อื่นใน workspace
go work use ./cli

# Sync go.work.sum (lock file สำหรับ workspaces)
go work sync
```

`go.work` มักถูก git-ignore (มันเป็น local developer convenience ไม่ใช่ deployment artefact) ใช้ `GOWORK=off` เพื่อ disable มันใน CI ถ้าจำเป็น

export const quizQuestionsModules = [
  { q: 'Go เทียบเท่า package-lock.json คืออะไร?', options: ['go.lock', 'go.sum', 'go.work', 'go.deps'], answer: 1 },
  { q: 'Go จัดการ v2+ breaking changes ใน module imports อย่างไร?', options: ['ใช้ @ version suffix บน imports', 'Major version ถูก embed ใน import path (/v2)', 'เพิ่ม field "version" ใน go.mod imports', 'ใช้ go-v2.mod file แยกต่างหาก'], answer: 1 },
  { q: 'คำสั่งใดที่ลบ dependencies ที่ไม่ใช้ออกจาก go.mod และ go.sum?', options: ['go clean', 'go mod tidy', 'go get -prune', 'go mod clean'], answer: 1 },
  { q: 'จุดประสงค์ของ replace directive ใน go.mod คืออะไร?', options: ['Pin dependency ไปยัง specific commit', 'Redirect module path ไปยัง local directory สำหรับ development', 'Rename module ใน import path', 'Vendor dependencies ทั้งหมด'], answer: 1 },
];

<Quiz client:visible id="advanced/modules-deep" questions={quizQuestionsModules} />

<ProgressTracker client:visible id="advanced/modules-deep" />
