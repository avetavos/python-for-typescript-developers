---
title: Docker Multi-Stage Builds
description: สร้าง Go container image ขนาดเล็กและปลอดภัยด้วย multi-stage Docker builds — เทียบกับ Node.js Docker image ทั่วไปที่มี node_modules
---

import TsGo from '../../../../components/TsGo.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## ปัญหา node_modules ใน Docker

เมื่อคุณ containerise แอปพลิเคชัน Node/TypeScript image สุดท้ายต้องมี:

1. Node.js runtime เต็มรูปแบบ (100+ MB)
2. `node_modules/` (สำหรับ NestJS app ทั่วไปอาจถึง 300–600 MB)
3. output `.js` ที่คอมไพล์แล้ว

ผลลัพธ์มักเป็น image ขนาด 500 MB–1 GB ที่ส่ง development dependency tree ทั้งหมดไปยัง production คุณลดสิ่งนี้ด้วย `.dockerignore`, `--omit=dev` และ multi-stage builds แต่ Node runtime เองไม่มีทางหายไปได้

Go คอมไพล์เป็น **binary เดียวแบบ statically-linked** โดยไม่ต้องพึ่ง runtime Docker image สุดท้ายสามารถ build `FROM scratch` — base image ที่ว่างเปล่าจริง ๆ — ที่มีแค่ binary ของคุณ Go production image ทั่วไปมีขนาด **5–20 MB**

## Dockerfile multi-stage สำหรับ Go

Multi-stage build ใช้ Go builder image เต็มรูปแบบในการคอมไพล์ แล้ว copy เฉพาะ binary ที่ได้ไปยัง final image ขนาดเล็ก

```dockerfile
# ---- Stage 1: Build ----
FROM golang:1.22-alpine AS builder

WORKDIR /app

# Copy dependency manifests ก่อนเพื่อ layer caching
COPY go.mod go.sum ./
RUN go mod download

# Copy source และ build static binary
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o /app/server ./cmd/server

# ---- Stage 2: Run ----
FROM gcr.io/distroless/static-debian12

WORKDIR /app

# Copy เฉพาะ binary จาก builder stage
COPY --from=builder /app/server .

EXPOSE 8080

ENTRYPOINT ["/app/server"]
```

`CGO_ENABLED=0` ปิดการใช้ C bindings เพื่อให้ binary เป็น self-contained อย่างสมบูรณ์ `-ldflags="-s -w"` strip debug symbols ลดขนาด binary ประมาณ ~30% `distroless/static` คือ minimal image ที่ Google ดูแลซึ่งมีเฉพาะ CA certificates และ timezone data — มีประโยชน์สำหรับ HTTPS calls — แต่ไม่มี shell, ไม่มี package manager, ไม่มี attack surface

ถ้าต้องการ shell สำหรับ debug ให้เปลี่ยน `distroless/static-debian12` เป็น `alpine:3.20` (ยังแค่ ~7 MB) และเพิ่ม `apk add --no-cache ca-certificates`

## Dockerfile multi-stage สำหรับ Node (เพื่อเปรียบเทียบ)

```dockerfile
# ---- Stage 1: Build ----
FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build      # tsc output → dist/

# Prune dev dependencies
RUN npm prune --omit=dev

# ---- Stage 2: Run ----
FROM node:20-alpine

WORKDIR /app

# ต้อง copy node_modules — runtime ยังต้องการมัน
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/package.json .

EXPOSE 3000

CMD ["node", "dist/main.js"]
```

แม้หลัง prune dev deps แล้ว Node runtime และ `node_modules` ที่เหลือทำให้ image ยังมีขนาด 150–300 MB

export const tsDockerCode = `# ขนาด Node image สุดท้าย: ~150-300 MB
# มี: node runtime + node_modules + dist/

FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
RUN npm prune --omit=dev

FROM node:20-alpine
WORKDIR /app
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist
EXPOSE 3000
CMD ["node", "dist/main.js"]`;

export const goDockerCode = `# ขนาด Go image สุดท้าย: ~5-20 MB
# มี: binary เท่านั้น (ไม่มี runtime)

FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux \\
    go build -ldflags="-s -w" \\
    -o /app/server ./cmd/server

FROM gcr.io/distroless/static-debian12
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
ENTRYPOINT ["/app/server"]`;

<TsGo client:visible ts={tsDockerCode} go={goDockerCode} />

> เหล่านี้คือตัวอย่าง Dockerfile — รันใน terminal ด้วย `docker build -t myapp .` แล้วใช้ `docker images myapp` เพื่อเปรียบเทียบขนาด ไม่มี browser playground สำหรับส่วนนี้

<GoOnly title="FROM scratch — image ที่ว่างเปล่า">

คุณสามารถไปไกลกว่า `distroless` และใช้ `FROM scratch` ซึ่งเป็น Docker image ที่ว่างเปล่าอย่างสมบูรณ์ binary ของคุณคือไฟล์เดียวใน container

สิ่งนี้ทำงานได้เพราะ Go binary แบบ statically linked — มันพกพาทุกอย่างที่ต้องการไว้ใน ELF/Mach-O/PE file เดียว ไม่มี dynamic linker, ไม่มี libc, ไม่มี shared libraries

Trade-off คือ `scratch` ไม่มี shell และไม่มี CA certificates ดังนั้นคุณต้อง `COPY --from=builder /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/` ถ้าแอปทำ HTTPS calls ขาออก `distroless/static` จัดการสิ่งนี้ให้คุณ ซึ่งเป็นเหตุผลที่มันเป็นตัวเลือกที่ใช้งานได้จริงกว่าสำหรับ service ส่วนใหญ่

</GoOnly>

export const quizQuestionsDocker = [
  { q: '`CGO_ENABLED=0` ทำอะไรเมื่อ build Go binary สำหรับ Docker?', options: ['เปิดใช้ C extensions เพื่อประสิทธิภาพดีขึ้น', 'ปิด C bindings เพื่อสร้าง binary แบบ fully static', 'ตั้งค่า target OS เป็น Linux', 'Strip debug symbols จาก binary'], answer: 1 },
  { q: 'ทำไม Go Docker image ถึงใช้ `FROM scratch` ได้ในขณะที่ Node image ไม่ได้?', options: ['Go image ถูกบีบอัดได้ดีกว่า', 'Go binary แบบ statically linked ไม่ต้องการ runtime', 'scratch รองรับเฉพาะ Linux', 'Node image ต้องการ database driver'], answer: 1 },
  { q: '`-ldflags="-s -w"` ใน Go build command มีจุดประสงค์อะไร?', options: ['เปิด link-time optimization เพื่อความเร็ว', 'Strip debug symbols เพื่อลดขนาด binary', 'ตั้งชื่อ output binary', 'บังคับ static linking บน Windows'], answer: 1 },
  { q: 'Go production image ไหนที่แนะนำสำหรับ service ที่ต้องการ CA certificates?', options: ['FROM scratch', 'FROM ubuntu:latest', 'FROM gcr.io/distroless/static-debian12', 'FROM golang:1.22'], answer: 2 },
];

<Quiz client:visible id="tooling/docker-multistage" questions={quizQuestionsDocker} />

<ProgressTracker client:visible id="tooling/docker-multistage" />
