---
title: Hello World — โปรแกรม Python แรกของคุณ
description: เขียนและรัน Python script แรก ทำความเข้าใจ print() และเรียนรู้ if __name__ == "__main__" entry-point guard
---
import TsGo from '../../../../components/TsGo.astro';
import Playground from '../../../../components/Playground.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import PyOnly from '../../../../components/PyOnly.astro';

## โปรแกรมที่ง่ายที่สุด

ใน TypeScript คุณมักต้องเซ็ตโปรเจกต์ก่อนถึงจะเขียนโค้ดบรรทัดแรกได้ ส่วนใน Python จากศูนย์ถึงรันได้จริงใช้แค่ไฟล์เดียวกับคำสั่งเดียว

export const helloTs = `// hello.ts
const name: string = "TypeScript Developer";
console.log(\`Hello, \${name}!\`);

// Run: npx ts-node hello.ts
// Output: Hello, TypeScript Developer!`;

export const helloPy = `# hello.py
name: str = "TypeScript Developer"
print(f"Hello, {name}!")

# Run: python3 hello.py
# Output: Hello, TypeScript Developer!`;

<TsGo ts={helloTs} go={helloPy} />

## ฟังก์ชัน `print`

`console.log` ของ TypeScript เทียบได้กับ `print` ของ Python ทั้งคู่รับหลาย argument เหมือนกัน แต่รายละเอียดต่างกันอยู่:

export const printTs = `// TypeScript console.log
console.log("a", "b", "c");          // a b c (space separated)
console.log("count:", 42);           // count: 42
console.log(\`sum = \${1 + 2}\`);       // sum = 3 (template literal)`;

export const printPy = `# Python print
print("a", "b", "c")                 # a b c (space separated)
print("count:", 42)                  # count: 42
print(f"sum = {1 + 2}")              # sum = 3 (f-string)

# print has keyword args:
print("a", "b", sep="-")            # a-b  (custom separator)
print("loading", end="...")         # loading... (no newline)`;

<TsGo ts={printTs} go={printPy} />

## F-strings — template literals ของ Python

f-string ของ Python (`f"..."`) ทำหน้าที่เดียวกับ template literal ของ TypeScript (`` `...` ``) วิธีใช้คือเติม `f` ไว้หน้า string แล้ววาง expression ไว้ใน `{}`

export const fstringTs = `// TypeScript template literal
const user = { name: "Alice", age: 30 };
const msg = \`\${user.name} is \${user.age} years old\`;
console.log(msg);  // Alice is 30 years old

// Format number
const pi = 3.14159;
console.log(\`pi ≈ \${pi.toFixed(2)}\`);  // pi ≈ 3.14`;

export const fstringPy = `# Python f-string
user = {"name": "Alice", "age": 30}
msg = f"{user['name']} is {user['age']} years old"
print(msg)   # Alice is 30 years old

# Format specifier inside {}
pi = 3.14159
print(f"pi ≈ {pi:.2f}")   # pi ≈ 3.14

# Any expression works
print(f"2 + 2 = {2 + 2}")  # 2 + 2 = 4`;

<TsGo ts={fstringTs} go={fstringPy} />

## การรัน script

export const runfileTs = `# TypeScript
# 1. Compile and run
tsc hello.ts && node hello.js

# 2. Direct with ts-node
npx ts-node hello.ts

# 3. Via npm script (package.json)
npm start`;

export const runfilePy = `# Python
# 1. Direct — no compilation step
python3 hello.py

# 2. Make the file executable (Unix)
chmod +x hello.py   # add #!/usr/bin/env python3 as line 1
./hello.py

# 3. Run as a module (inside a package)
python3 -m mypackage.hello`;

<TsGo ts={runfileTs} go={runfilePy} />

## Entry-point guard: `if __name__ == "__main__":`

นี่คือ pattern ของ Python ที่ไม่มีตัวเทียบตรง ๆ ใน TypeScript จึงควรอธิบายให้ละเอียดสักหน่อย

เวลา Python รันไฟล์โดยตรงด้วย `python3 hello.py` interpreter จะตั้ง `__name__` ของ module นั้นเป็น string `"__main__"` แต่ถ้าไฟล์อื่นเรียก `import hello` ค่า `__name__` จะกลายเป็น `"hello"` ซึ่งก็คือชื่อ module แทน

ดังนั้น `if __name__ == "__main__":` จึงแปลว่า "รันโค้ดในนี้เฉพาะตอนไฟล์นี้เป็น entry point อย่าไปรันตอนโดน import"

export const guardTs = `// TypeScript: no direct equivalent
// In Node, top-level code always runs when the file is imported
// You work around this with explicit exports:

export function greet(name: string) {
  return \`Hello, \${name}!\`;
}

// This runs unconditionally on import — often undesirable:
// console.log(greet("world"));`;

export const guardPy = `# Python: the entry-point guard
def greet(name: str) -> str:
    return f"Hello, {name}!"

if __name__ == "__main__":
    # This block ONLY runs when you do: python3 hello.py
    # It does NOT run when another file does: import hello
    print(greet("world"))`;

<TsGo ts={guardTs} go={guardPy} />

<PyOnly title="ทำไม __name__ ถึงสำคัญ?">

ลองนึกถึงไฟล์ `utils.py` ที่นิยาม helper ไว้ แล้วดันมีบรรทัด print debug message ติดมาด้วย พอ module อื่น import เข้าไป debug message นั้นก็จะโผล่ขึ้นมาแบบไม่ได้ตั้งใจ ตัว guard `if __name__ == "__main__":` แก้ปัญหานี้ให้ เพราะมีแค่ไฟล์ที่เป็น entry point เท่านั้นที่ได้รัน block นั้น

เหตุผลเดียวกันนี้เองที่ทำให้ test runner อย่าง `pytest` import source file ของคุณเข้าไปได้โดยไม่ไป trigger `main()` เพราะ guard กันไม่ให้ side effect หลุดออกมา

Best practice คือ ห่อโค้ดที่ต้องรันจริงในระดับบนสุดไว้ใน guard นี้ทั้งหมด

</PyOnly>

## Script แรกฉบับสมบูรณ์

export const completeHelloCode = `# hello.py — a complete first Python script

def greet(name: str) -> str:
    """Return a personalised greeting."""
    return f"Hello, {name}!"

def main() -> None:
    names = ["Alice", "Bob", "TypeScript Developer"]
    for name in names:
        message = greet(name)
        print(message)

    # f-string formatting
    count = len(names)
    print(f"\\nGreeted {count} people.")

if __name__ == "__main__":
    main()
`;

<Playground code={completeHelloCode} />

export const quizHello = [
  {
    q: '`console.log` ใน Python คืออะไร?',
    options: [
      '`log()`',
      '`echo()`',
      '`print()`',
      '`stdout()`',
    ],
    answer: 2,
  },
  {
    q: '`__name__` มีค่าเป็นอะไรเมื่อรัน file โดยตรงด้วย `python3 hello.py`?',
    options: [
      '`"hello"`',
      '`"hello.py"`',
      '`None`',
      '`"__main__"`',
    ],
    answer: 3,
  },
  {
    q: 'Python string prefix ไหนสร้างสิ่งเทียบเท่า template literal?',
    options: [
      '`t"..."`',
      '`f"..."`',
      '`r"..."`',
      '`b"..."`',
    ],
    answer: 1,
  },
  {
    q: 'เกิดอะไรขึ้นกับ block `if __name__ == "__main__":` เมื่อ module อื่น import ไฟล์นี้?',
    options: [
      'raise ImportError',
      'รันครั้งเดียวตอน import ครั้งแรก',
      'ถูกข้ามทั้ง block — ไม่รันเลย',
      'รันแยกใน subprocess',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="intro/hello-world" questions={quizHello} />

<ProgressTracker client:visible id="intro/hello-world" />
