---
title: Interfaces และการ Satisfy แบบ Implicit
description: interfaces และการ satisfy แบบ implicit ของ Go เทียบกับ structural typing และ explicit implements ของ TypeScript
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Interfaces: เป็น structural แต่ implicit

TypeScript ใช้ **structural typing** — ถ้า type มีรูปร่างที่ถูกต้อง มันก็เข้ากันได้ interface ของ Go ทำงานในแบบเดียวกัน แต่มีความแตกต่างสำคัญหนึ่งอย่าง: **คุณไม่ต้องเขียน `implements` เลย** ถ้า type หนึ่งมีเมธอดครบทุกตัวที่ interface ต้องการ มันก็ satisfy interface นั้นโดยอัตโนมัติ สิ่งนี้เรียกว่า **implicit satisfaction**

## การนิยามและการ satisfy interface

export const tsInterfaceCode = `interface Greeter {
    greet(): string;
}

// TypeScript: no implements needed (structural)
class User {
    constructor(public name: string) {}
    greet(): string { return \`Hello from \${this.name}\`; }
}

function sayHello(g: Greeter) {
    console.log(g.greet());
}`;

export const goInterfaceCode = `type Greeter interface {
    Greet() string
}

type User struct { Name string }

// No "implements" — User satisfies Greeter automatically
func (u User) Greet() string {
    return "Hello from " + u.Name
}

func sayHello(g Greeter) {
    fmt.Println(g.Greet())
}`;

<TsGo client:visible ts={tsInterfaceCode} go={goInterfaceCode} />

## พลังของมัน: แยกการนิยามออกจากการ implement

เพราะการ satisfy เป็นแบบ implicit interface จึงสามารถถูกนิยามไว้ใน package ที่ **ใช้งาน** มันได้ ไม่ใช่ใน package ที่ให้ implementation นั่นหมายความว่าผู้เขียน library ไม่จำเป็นต้องรู้เกี่ยวกับ interface ของคุณ — ถ้า type ของเขามีเมธอดที่ถูกต้อง มันก็ใช้งานได้

export const tsDepsCode = `// TypeScript — interface often lives with the class
interface Logger {
    log(msg: string): void;
}
class ConsoleLogger implements Logger {
    log(msg: string) { console.log(msg); }
}`;

export const goDepsCode = `// Go — interface lives with the consumer
// (in the package that needs it, not the one that provides it)
type Logger interface {
    Log(msg string)
}

// This type in another package doesn't know about Logger
type ConsoleLogger struct{}
func (c ConsoleLogger) Log(msg string) { fmt.Println(msg) }
// ConsoleLogger satisfies Logger without knowing Logger exists`;

<TsGo client:visible ts={tsDepsCode} go={goDepsCode} />

## ลองเล่นดู

export const playgroundIfaceCode = `package main

import (
	"fmt"
	"math"
)

type Shape interface {
	Area() float64
	Perimeter() float64
}

type Circle struct{ Radius float64 }
type Rect struct{ Width, Height float64 }

func (c Circle) Area() float64      { return math.Pi * c.Radius * c.Radius }
func (c Circle) Perimeter() float64 { return 2 * math.Pi * c.Radius }

func (r Rect) Area() float64      { return r.Width * r.Height }
func (r Rect) Perimeter() float64 { return 2 * (r.Width + r.Height) }

func describe(s Shape) {
	fmt.Printf("Area: %.2f, Perimeter: %.2f\\n", s.Area(), s.Perimeter())
}

func main() {
	shapes := []Shape{
		Circle{Radius: 5},
		Rect{Width: 4, Height: 3},
	}
	for _, s := range shapes {
		describe(s)
	}
}`;

<Playground client:visible code={playgroundIfaceCode} />

export const quizQuestionsIface = [
  { q: 'type ใน Go ส่งสัญญาณว่ามัน implement interface ได้อย่างไร?', options: ['ด้วยการเขียน "implements InterfaceName"', 'ด้วยการ embed interface', 'โดยอัตโนมัติ — ด้วยการมีเมธอดที่จำเป็น', 'ด้วยการลงทะเบียนกับ factory'], answer: 2 },
  { q: 'ที่ใดคือที่ที่ idiomatic ในการนิยาม interface ของ Go?', options: ['ใน package ที่ให้ implementation', 'ใน package ที่ใช้งานมัน', 'ใน package types ที่ใช้ร่วมกันเสมอ', 'ข้างๆ main()'], answer: 1 },
  { q: 'structural typing ของ TypeScript กับการ satisfy interface แบบ implicit ของ Go นั้น:', options: ['เป็นแนวคิดที่ต่างกันโดยสิ้นเชิง', 'คล้ายกัน — ทั้งคู่จับคู่ตามรูปร่าง แต่ Go ไม่มีคีย์เวิร์ด implements', 'เหมือนกันเป๊ะ — Go แค่ลอก TypeScript มา', 'ทั้งคู่ต้องประกาศอย่างชัดเจน'], answer: 1 },
  { q: 'empty interface ใน Go คืออะไร (type ที่ทุก type satisfy ได้)?', options: ['interface{}  or  any', 'object', 'unknown', 'void'], answer: 0 },
];

<Quiz client:visible id="go-101/interfaces" questions={quizQuestionsIface} />

<ProgressTracker client:visible id="go-101/interfaces" />
